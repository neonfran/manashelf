{
  "name": "manashelf",
  "version": "2.5.57-beta.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace client",
    "postinstall": "npm run build --workspace client",
    "start": "node server/index.mjs"
  }
}
