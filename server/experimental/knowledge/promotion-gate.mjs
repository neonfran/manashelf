import {PROMOTION_GATE_VERSION} from "./schema.mjs";
export const DEFAULT_PROMOTION_POLICY=Object.freeze({minStressCompleted:200,minUnseenCompleted:60,minComparablePairs:30,minNoWorseRate:.80,minMeanDelta:0,maxSevereRegressions:0});
export function evaluatePromotionGate({stress,unseen,regression,knowledge,lab2Hashes,field,policy={}}={}){const p={...DEFAULT_PROMOTION_POLICY,...policy},checks={stress:Boolean(stress&&stress.completed>=p.minStressCompleted&&Number(stress.failed||0)===0&&Number(stress.anomalous||0)===0),unseen:Boolean(unseen&&unseen.completed>=p.minUnseenCompleted&&Number(unseen.failed||0)===0&&Number(unseen.anomalous||0)===0),regression:Boolean(regression===true||regression?.pass===true||regression?.status==="PASS"),knowledge:Boolean(knowledge===true||knowledge?.integrity==="PASS"||knowledge?.ok===true),lab2Hashes:Boolean(lab2Hashes===true||lab2Hashes?.pass===true||lab2Hashes?.status==="PASS"),fieldPairs:Number(field?.pairs||0)>=p.minComparablePairs,noWorse:Number(field?.noWorseRate||0)>=p.minNoWorseRate,meanDelta:Number(field?.meanDelta??-Infinity)>=p.minMeanDelta,severe:Number(field?.severeRegressions||0)<=p.maxSevereRegressions};const reasons=Object.entries(checks).filter(([,ok])=>!ok).map(([name])=>name);return {version:PROMOTION_GATE_VERSION,decision:reasons.length?"HOLD":"PROMOTE",checks,reasons,policy:p,evidence:{stress,unseen,field}};}
